/**
 * Legacy compatibility entrypoint.
 *
 * Upstream auth enforcement monkey-patched app-local modules. In this extracted
 * shared package we intentionally avoid cross-package runtime patching.
 */
export {};
